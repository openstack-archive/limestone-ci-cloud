---

container_skel:
  cinder_api_container:
    belongs_to:
      - storage-infra_containers
    contains:
      - cinder_api
      - cinder_scheduler
  cinder_scheduler_container: {}
  cinder_volumes_container:
    properties:
      is_metal: false
